name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - run: pip install -r requirements.txt
      
      - env:
          DATABASE_URL: postgresql://neondb_owner:npg_BHC94ZFovLws@ep-odd-dew-aigjwtwa-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
        run: python manage.py test sox_controls itgc --keepdb
